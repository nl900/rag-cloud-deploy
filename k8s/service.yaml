apiVersion: v1
kind: Service
metadata:
  name: rag-service
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "8000"
spec:
  type: ClusterIP
  selector:
    app: rag-app
  ports:
    - port: 80
      targetPort: 8000
